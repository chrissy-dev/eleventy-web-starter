{
  "name": "web-starter",
  "version": "1.0.0",
  "description": "Basic web starter kit with 11ty, TailwindsCSS and PostCSS.",
  "scripts": {
    "post": "postcss src/assets/stylesheets/app.css -o src/assets/static/app.compiled.css -w",
    "start": "concurrently \"ELEVENTY_ENV=development eleventy --serve --watch\" \"npm run post\"",
    "build": "del dist && eleventy && purgecss --config ./purgecss.config.js --out src/_includes/stylesheets && ELEVENTY_ENV=production eleventy"
  },
  "author": "Chris Collins",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.8.3",
    "autoprefixer": "^9.4.7",
    "concurrently": "^4.1.0",
    "del-cli": "^2.0.0",
    "postcss-cli": "^6.1.2",
    "purgecss": "^1.1.0",
    "tailwindcss": "^1.0.3"
  }
}
